<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.myapp.repository.MemberRepository">

    <insert id="insertMember">
        INSERT INTO SHOP_MEMBER_TB(MEMBER_ID, MEMBER_PWD, MEMBER_LEVEL,
                                   MEMBER_NAME,MEMBER_EMAIL, MEMBER_ADDRESS,
                                   MEMBER_TEL, MEMBER_BIRTH,
                                   BUSINESS_REGISTRATION_NO, BUSINESS_NAME)
        VALUES (#{memberId}, #{memberPwd}, #{memberLevel, jdbcType=VARCHAR},
                #{memberName},#{memberEmail}, #{memberAddress}, #{memberTel}, #{memberBirth}
                   , #{businessRegistrationNo,jdbcType=VARCHAR}, #{businessName,jdbcType=VARCHAR})
    </insert>
    <update id="updateMember">
        UPDATE SHOP_MEMBER_TB
        SET MEMBER_PWD   = #{memberPwd},
            MEMBER_NAME=#{memberName},
            MEMBER_ADDRESS=#{memberAddress},
            MEMBER_TEL=#{memberTel},
            MEMBER_BIRTH=#{memberBirth}
        WHERE MEMBER_ID = #{memberId}
    </update>
    <select id="findAll" resultType="com.shop.myapp.dto.Member" >
        SELECT *
        FROM SHOP_MEMBER_TB
        <if test ="condition !=null and condition !='' ">
         WHERE ${chkInfo} LIKE '%'||#{condition}||'%'
        </if>
    </select>
    <select id="findById" resultType="com.shop.myapp.dto.Member">
        SELECT *
        FROM SHOP_MEMBER_TB
        WHERE MEMBER_ID = #{memberId}
    </select>
</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.myapp.repository.MemberRepository">

    <insert id="insertMember">
<<<<<<< HEAD:src/main/resources/countryRepository/MemberSQL.xml
       		INSERT INTO SHOP_MEMBER_TB(member_id,member_pwd,MEMBER_NAME,MEMBER_ADdRESS,MEMBER_TEL,MEMBER_BIRTH) 
       		VALUES(#{memberId}, #{memberPwd},
       		#{memberName}, #{memberAddress}, #{memberTel}, #{memberBirth})
    </insert>
    <insert id="insertSellerInfo">
       		INSERT INTO SHOP_SELLER_TB VALUES(#{memberId}, #{businessRegistrationNo}, #{businessName})
    </insert>
    <update id="updateMember">
    		UPDATE SHOP_MEMBER_TB SET MEMBER_PWD = #{memberPwd}, MEMBER_NAME=#{memberName},
    		MEMBER_ADDRESS=#{memberAddress},MEMBER_TEL=#{memberTel},MEMBER_BIRTH=#{memberBirth}
    		WHERE MEMBER_ID = #{memberId}
        INSERT INTO SHOP_MEMBER_TB(member_id, member_pwd,
                                   member_name, member_address,
                                   member_tel, member_birth,
=======
        INSERT INTO SHOP_MEMBER_TB(MEMBER_ID, MEMBER_PWD, MEMBER_LEVEL,
                                   MEMBER_NAME,MEMBER_EMAIL, MEMBER_ADDRESS,
                                   MEMBER_TEL, MEMBER_BIRTH,
>>>>>>> 875ab7ec0071a66b6a56e3ea61a39b6460440f90:src/main/resources/mapper/MemberSQL.xml
                                   BUSINESS_REGISTRATION_NO, BUSINESS_NAME)
        VALUES (#{memberId}, #{memberPwd}, #{memberLevel, jdbcType=VARCHAR},
                #{memberName},#{memberEmail}, #{memberAddress}, #{memberTel}, #{memberBirth}
                   , #{businessRegistrationNo,jdbcType=VARCHAR}, #{businessName,jdbcType=VARCHAR})
    </update>

    <update id="updateMember">
        UPDATE SHOP_MEMBER_TB
        SET MEMBER_PWD   = #{memberPwd},
            MEMBER_NAME=#{memberName},
            MEMBER_ADDRESS=#{memberAddress},
            MEMBER_TEL=#{memberTel},
            MEMBER_BIRTH=#{memberBirth}
        WHERE MEMBER_ID = #{memberId}

    </update>
    <update id="updateSeller" >
        update HR.SHOP_MEMBER_TB
set HR.SHOP_MEMBER_TB.BUSINESS_NAME = #{businessName}, business_info = #{businessInfo} where member_id = #{memberId}
    </update>
    <select id="findAll" resultType="com.shop.myapp.dto.Member" >
        SELECT *
        FROM SHOP_MEMBER_TB
        <if test ="condition !=null and condition !='' ">
         WHERE ${chkInfo} LIKE '%'||#{condition}||'%'
        </if>
    </select>
    <select id="findById" resultType="com.shop.myapp.dto.Member">
        SELECT *
        FROM SHOP_MEMBER_TB
        WHERE MEMBER_ID = #{memberId}
    </select>


</mapper>
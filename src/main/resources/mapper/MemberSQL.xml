<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.myapp.repository.MemberRepository">

    <insert id="insertMember">
--         INSERT INTO SHOP_MEMBER_TB VALUES(
			${member_id}, ${member_pwd}, ${member_level},     
			${member_name}, ${member_adress}, ${member_tel}, ${member_birth})
    </insert>
    <select id="findAll" resultType="com.shop.myapp.dto.Member">
--         select 실행
    </select>
</mapper>
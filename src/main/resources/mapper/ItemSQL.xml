<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.myapp.repository.ItemRepository">
    <insert id="insertItem" parameterType="com.shop.myapp.dto.Item">
        <selectKey resultType="java.lang.String" keyProperty="itemCode" order="BEFORE">
            SELECT CODE_ITEM_SQ.nextval FROM DUAL
        </selectKey>
        insert into HR.SHOP_ITEM_TB(item_code,
        member_id,
        country_code,
        item_name,
        item_cost,
        item_image,
        item_info)
        values (#{itemCode},
        #{memberId},
        #{countryCode},
        #{itemName},
        #{itemPrice},
        #{itemImage},
        #{itemInfo})
    </insert>
    <update id="updateItem" parameterType="com.shop.myapp.dto.Item">

    </update>
    <delete id="deleteItem" parameterType="string">
        delete HR.SHOP_ITEM_TB
        where HR.SHOP_ITEM_TB.ITEM_CODE = #{itemCode}
    </delete>

    <select id="findAll" resultType="com.shop.myapp.dto.Item">
        select item.*, sum(item_option.OPTION_STOCK)
        from HR.SHOP_ITEM_TB item,
             HR.SHOP_ITEM_OPTIONS_TB item_option
        where item.ITEM_CODE = item_option.ITEM_CODE
        group by item.ITEM_CODE, MEMBER_ID, COUNTRY_CODE, ITEM_NAME, ITEM_COST, ITEM_IMAGE, ITEM_INFO, ITEM_STOCK
    </select>
    <select id="findByItemCode" resultType="com.shop.myapp.dto.Item">
        select *
        from HR.SHOP_ITEM_TB
        where ITEM_CODE = #{itemCode}
    </select>
</mapper>